import React from "react";
// step 1: create context
export const UseContext = React.createContext();

///provider, consumer(we will use hooks)

// step 1: create context
// step 2: wrap childs with context provider
// step 3: state access useContext hook
